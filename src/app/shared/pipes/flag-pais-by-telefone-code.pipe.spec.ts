import { FlagPaisByTelefoneCodePipe } from './flag-pais-by-telefone-code.pipe';

describe('FlagPaisByTelefoneCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FlagPaisByTelefoneCodePipe();
    expect(pipe).toBeTruthy();
  });
});
